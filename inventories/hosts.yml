regensburg:
  hosts:
    mserver:
      ansible_host: 100.78.131.48
  vars:
    ansible_user: "{{vault_ansible_user}}"
    ansible_password: "{{vault_ansible_password}}"

monitor:
  hosts:
    pimonitor:
      ansible_host: 100.72.180.59
  vars:
    ansible_user: "{{vault_ansible_user}}"
    ansible_password: "{{vault_ansible_password}}"
    ansible_python_interpreter: /usr/bin/python3

